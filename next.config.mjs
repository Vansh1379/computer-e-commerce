/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'unique.rightinfoservice.com',
      'i.pinimg.com',
      'images.unsplash.com',
      'via.placeholder.com',
      "m.media-amazon.com"
    ],
  },
};

export default nextConfig;
